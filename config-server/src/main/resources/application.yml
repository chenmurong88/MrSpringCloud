server:
  port: 8300

spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths: config
          default-label: master

